<UserControl x:Class="POS.CustomControl.CompanyInfo_UserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:validations="clr-namespace:POS.Validations"
             mc:Ignorable="d" MinWidth="800" MinHeight="800">
    
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Assets/UltraModernStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" Background="{StaticResource BackgroundGradientBrush}">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" CornerRadius="20" Padding="28,20" Margin="0,0,0,20">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#1E40AF" Offset="0"/>
                        <GradientStop Color="#0EA5E9" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
                <StackPanel>
                    <TextBlock Text="معلومات الشركة" FontSize="24" FontWeight="Bold" Foreground="White" FlowDirection="RightToLeft"/>
                    <TextBlock Text="إعدادات بيانات الشركة والضرائب" FontSize="12" Foreground="#E0E7FF" Margin="0,4,0,0" FlowDirection="RightToLeft"/>
                </StackPanel>
            </Border>

            <!-- Form -->
            <Border Grid.Row="1" Style="{StaticResource UltraModernCard}" Padding="32,28">
                <StackPanel>
                    <!-- Section 1: Basic Info -->
                    <TextBlock Text="معلومات الشركة الأساسية" FontSize="16" FontWeight="Bold" Foreground="{StaticResource Gray900Brush}" Margin="0,0,0,16" FlowDirection="RightToLeft"/>
                    <Grid Margin="0,0,0,20">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="16"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="16"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Row="0" Grid.Column="0">
                            <TextBlock Text="اسم الشركة" FontSize="13" FontWeight="SemiBold" Foreground="{StaticResource Gray700Brush}" Margin="0,0,0,8" FlowDirection="RightToLeft"/>
                            <TextBox Text="{Binding CompanyName}" Style="{StaticResource UltraModernTextBox}"/>
                        </StackPanel>

                        <StackPanel Grid.Row="0" Grid.Column="2">
                            <TextBlock Text="الاسم التجاري" FontSize="13" FontWeight="SemiBold" Foreground="{StaticResource Gray700Brush}" Margin="0,0,0,8" FlowDirection="RightToLeft"/>
                            <TextBox Text="{Binding TradeName}" Style="{StaticResource UltraModernTextBox}"/>
                        </StackPanel>

                        <StackPanel Grid.Row="2" Grid.Column="0">
                            <TextBlock Text="الرقم الضريبي" FontSize="13" FontWeight="SemiBold" Foreground="{StaticResource Gray700Brush}" Margin="0,0,0,8" FlowDirection="RightToLeft"/>
                            <TextBox Text="{Binding TaxNumber}" Style="{StaticResource UltraModernTextBox}"/>
                        </StackPanel>

                        <StackPanel Grid.Row="2" Grid.Column="2">
                            <TextBlock Text="السجل التجاري" FontSize="13" FontWeight="SemiBold" Foreground="{StaticResource Gray700Brush}" Margin="0,0,0,8" FlowDirection="RightToLeft"/>
                            <TextBox Text="{Binding CommercialRegister}" Style="{StaticResource UltraModernTextBox}"/>
                        </StackPanel>
                    </Grid>

                    <!-- Section 2: Contact Info -->
                    <TextBlock Text="معلومات الاتصال" FontSize="16" FontWeight="Bold" Foreground="{StaticResource Gray900Brush}" Margin="0,0,0,16" FlowDirection="RightToLeft"/>
                    <Grid Margin="0,0,0,20">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="16"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="16"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Row="0" Grid.Column="0">
                            <TextBlock Text="الهاتف" FontSize="13" FontWeight="SemiBold" Foreground="{StaticResource Gray700Brush}" Margin="0,0,0,8" FlowDirection="RightToLeft"/>
                            <TextBox Text="{Binding Phone}" Style="{StaticResource UltraModernTextBox}"/>
                        </StackPanel>

                        <StackPanel Grid.Row="0" Grid.Column="2">
                            <TextBlock Text="البريد الإلكتروني" FontSize="13" FontWeight="SemiBold" Foreground="{StaticResource Gray700Brush}" Margin="0,0,0,8" FlowDirection="RightToLeft"/>
                            <TextBox Text="{Binding Email}" Style="{StaticResource UltraModernTextBox}"/>
                        </StackPanel>

                        <StackPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="3">
                            <TextBlock Text="العنوان" FontSize="13" FontWeight="SemiBold" Foreground="{StaticResource Gray700Brush}" Margin="0,0,0,8" FlowDirection="RightToLeft"/>
                            <TextBox Text="{Binding Address}" Style="{StaticResource UltraModernTextBox}"/>
                        </StackPanel>
                    </Grid>

                    <!-- Section 3: Logo -->
                    <TextBlock Text="شعار الشركة" FontSize="16" FontWeight="Bold" Foreground="{StaticResource Gray900Brush}" Margin="0,0,0,16" FlowDirection="RightToLeft"/>
                    <Grid Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0" Width="120" Height="120" CornerRadius="16" BorderBrush="{StaticResource Gray300}" BorderThickness="2" Margin="0,0,20,0">
                            <Image Source="{Binding Logo}" Stretch="Uniform"/>
                        </Border>

                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <Button Content="اختيار شعار" Style="{StaticResource UltraModernPrimaryButton}" Width="160" Command="{Binding SelectLogoCommand}"/>
                            <TextBlock Text="الحجم الموصى به: 512×512 بكسل" FontSize="11" Foreground="{StaticResource Gray500}" Margin="0,8,0,0" FlowDirection="RightToLeft"/>
                        </StackPanel>
                    </Grid>

                    <!-- Section 4: Tax Settings -->
                    <TextBlock Text="إعدادات الضرائب" FontSize="16" FontWeight="Bold" Foreground="{StaticResource Gray900Brush}" Margin="0,0,0,16" FlowDirection="RightToLeft"/>
                    <Grid Margin="0,0,0,28">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="16"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="نسبة الضريبة (%)" FontSize="13" FontWeight="SemiBold" Foreground="{StaticResource Gray700Brush}" Margin="0,0,0,8" FlowDirection="RightToLeft"/>
                            <TextBox Text="{Binding TaxPercentage}" Style="{StaticResource UltraModernTextBox}"/>
                        </StackPanel>

                        <StackPanel Grid.Column="2" VerticalAlignment="Bottom">
                            <CheckBox Content="تضمين الضريبة في الأسعار" FontSize="14" Foreground="{StaticResource Gray700Brush}" 
                                      IsChecked="{Binding IncludeTaxInPrice}" FlowDirection="RightToLeft" Margin="0,0,0,12"/>
                        </StackPanel>
                    </Grid>

                    <!-- Actions -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <Button Content="حفظ التغييرات" Style="{StaticResource UltraModernSuccessButton}" Width="180" Command="{Binding SaveCommand}"/>
                        <Button Content="إلغاء" Style="{StaticResource UltraModernDangerButton}" Width="140" Margin="12,0,0,0" Command="{Binding CancelCommand}"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>
    </ScrollViewer>
</UserControl>
