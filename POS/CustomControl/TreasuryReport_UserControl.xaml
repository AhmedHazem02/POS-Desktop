<UserControl x:Class="POS.CustomControl.TreasuryReport_UserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:validations="clr-namespace:POS.Validations"
             mc:Ignorable="d" MinWidth="800" MinHeight="800">
    
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Assets/UltraModernStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <validations:IndexPlusOneConverter x:Key="IndexPlusOneConverter"/>
        </ResourceDictionary>
    </UserControl.Resources>

    <UserControl.Triggers>
        <EventTrigger RoutedEvent="Loaded">
            <BeginStoryboard Storyboard="{StaticResource FadeInAnimation}"/>
        </EventTrigger>
    </UserControl.Triggers>

    <Grid Background="{StaticResource BackgroundGradientBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" CornerRadius="20" Margin="20,20,20,16" Padding="32,24">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#047857" Offset="0"/>
                    <GradientStop Color="#10B981" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
            <StackPanel>
                <TextBlock Text="تقرير الخزينة" FontSize="28" FontWeight="Bold" Foreground="White" FlowDirection="RightToLeft"/>
                <TextBlock Text="تقرير شامل للإيرادات والمصروفات" FontSize="13" Foreground="#D1FAE5" Margin="0,4,0,0" FlowDirection="RightToLeft"/>
            </StackPanel>
        </Border>

        <!-- Stats Cards -->
        <Grid Grid.Row="1" Margin="20,0,20,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource UltraModernCard}" Padding="20,18">
                <StackPanel>
                    <TextBlock Text="إجمالي الإيرادات" FontSize="12" Foreground="{StaticResource Gray600}" FlowDirection="RightToLeft"/>
                    <TextBlock Text="{Binding TotalIncome}" FontSize="28" FontWeight="ExtraBold" Foreground="{StaticResource Success600Brush}" Margin="0,8,0,0"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Style="{StaticResource UltraModernCard}" Padding="20,18">
                <StackPanel>
                    <TextBlock Text="إجمالي المصروفات" FontSize="12" Foreground="{StaticResource Gray600}" FlowDirection="RightToLeft"/>
                    <TextBlock Text="{Binding TotalExpenses}" FontSize="28" FontWeight="ExtraBold" Foreground="{StaticResource Danger600Brush}" Margin="0,8,0,0"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="4" Style="{StaticResource UltraModernCard}" Padding="20,18">
                <StackPanel>
                    <TextBlock Text="صافي الربح" FontSize="12" Foreground="{StaticResource Gray600}" FlowDirection="RightToLeft"/>
                    <TextBlock Text="{Binding NetProfit}" FontSize="28" FontWeight="ExtraBold" Foreground="{StaticResource Primary600Brush}" Margin="0,8,0,0"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="6" Style="{StaticResource UltraModernCard}" Padding="20,18">
                <StackPanel>
                    <TextBlock Text="رصيد الخزينة" FontSize="12" Foreground="{StaticResource Gray600}" FlowDirection="RightToLeft"/>
                    <TextBlock Text="{Binding Balance}" FontSize="28" FontWeight="ExtraBold" Foreground="{StaticResource Accent600Brush}" Margin="0,8,0,0"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Filters -->
        <Border Grid.Row="2" Style="{StaticResource UltraModernCard}" Margin="20,0,20,16" Padding="24,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="من تاريخ" FontSize="13" FontWeight="SemiBold" Foreground="{StaticResource Gray700Brush}" Margin="0,0,0,8" FlowDirection="RightToLeft"/>
                    <DatePicker SelectedDate="{Binding DateFrom}" Style="{StaticResource UltraModernDatePicker}"/>
                </StackPanel>

                <StackPanel Grid.Column="2">
                    <TextBlock Text="إلى تاريخ" FontSize="13" FontWeight="SemiBold" Foreground="{StaticResource Gray700Brush}" Margin="0,0,0,8" FlowDirection="RightToLeft"/>
                    <DatePicker SelectedDate="{Binding DateTo}" Style="{StaticResource UltraModernDatePicker}"/>
                </StackPanel>

                <Button Grid.Column="4" Content="إنشاء التقرير" Style="{StaticResource UltraModernSuccessButton}" 
                        Width="160" VerticalAlignment="Bottom" Command="{Binding GenerateReportCommand}"/>
            </Grid>
        </Border>

        <!-- Transactions DataGrid -->
        <Border Grid.Row="3" Style="{StaticResource UltraModernCard}" Margin="20,0,20,20" Padding="0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Background="{StaticResource Gray50}" Padding="24,16" BorderBrush="{StaticResource Gray200}" BorderThickness="0,0,0,1">
                    <TextBlock Text="تفاصيل المعاملات" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource Gray900Brush}" FlowDirection="RightToLeft"/>
                </Border>

                <DataGrid Grid.Row="1" ItemsSource="{Binding Transactions}" Style="{StaticResource UltraModernDataGrid}"
                          AutoGenerateColumns="False" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="#" Width="60" 
                                            Binding="{Binding RelativeSource={RelativeSource AncestorType=DataGridRow}, Converter={StaticResource IndexPlusOneConverter}}">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="TextAlignment" Value="Center"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                    <Setter Property="Foreground" Value="{StaticResource Gray500}"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTextColumn Header="التاريخ" Width="1.2*" Binding="{Binding Date, StringFormat='{}{0:dd/MM/yyyy}'}"/>
                        <DataGridTextColumn Header="الوصف" Width="3*" Binding="{Binding Description}"/>
                        <DataGridTextColumn Header="النوع" Width="1.2*" Binding="{Binding Type}"/>
                        <DataGridTextColumn Header="المبلغ" Width="1.5*" Binding="{Binding Amount}">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
