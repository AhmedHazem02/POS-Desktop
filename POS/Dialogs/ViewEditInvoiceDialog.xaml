<Window x:Class="POS.Dialogs.ViewEditInvoiceDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:fa="http://schemas.fontawesome.io/icons/" 
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:controls="clr-namespace:POS.Components"
        xmlns:converters="clr-namespace:POS.Validations"
        xmlns:Icon="http://schemas.fontawesome.io/icons/" 
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Height="600" Width="800" WindowState="Normal" WindowStyle="None" Background="White"
        AllowsTransparency="True">
    <Window.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <Style x:Key="IconButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource PrimaryTextColor}"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Margin" Value="5"/>
        </Style>

        <!-- ModernTextBoxStyle -->
        <Style x:Key="ModernTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#F7F7F7"/>
            <Setter Property="BorderBrush" Value="#DDDDDD"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="ToolTip" Value="{Binding ErrorMessage}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
              BorderBrush="{TemplateBinding BorderBrush}"
              BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="PART_ContentHost"
                        VerticalAlignment="Center"
                        Padding="{TemplateBinding Padding}"
                        SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                        Focusable="false"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="CustomButtonStyle" TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="PART_Border"
                         Cursor="Hand"
                 Background="{TemplateBinding Background}"
                 BorderBrush="{TemplateBinding BorderBrush}"
                 BorderThickness="{TemplateBinding BorderThickness}"
                         
                 CornerRadius="{StaticResource AppCornerRadius}" Width="{Binding Width, FallbackValue=auto}" Padding="5, 0" MinWidth="{Binding MinWidth, FallbackValue=100}" Height="35" Margin="5, 0">
                            <ContentPresenter x:Name="PART_ContentPresenter"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                                       TextElement.FontSize="14"
                                       TextElement.FontFamily="{StaticResource Montserrat_Semibold}"
                                       TextElement.FontStretch="Expanded"
                               TextElement.Foreground="{TemplateBinding Foreground}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="PART_Border" Property="Background" Value="#004FC4" />
                                <Setter TargetName="PART_ContentPresenter" Property="TextElement.Foreground" Value="White" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="PART_Border" Property="Background" Value="DarkGray" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="PART_Border" Property="Opacity" Value="0.5" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="CustomLabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="0 10 0 10"/>
            <Setter Property="FlowDirection" Value="RightToLeft"/>
        </Style>
        <Style x:Key="CustomInputStyle" TargetType="TextBox">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Margin" Value="0 0 0 10"/>
            <Setter Property="Width" Value="150"/>
            <Setter Property="FlowDirection" Value="LeftToRight"/>
        </Style>
        <Style x:Key="CustomDropdownStyle" TargetType="ComboBox">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Margin" Value="0 0 0 10"/>
            <Setter Property="Width" Value="150"/>
            <Setter Property="FlowDirection" Value="LeftToRight"/>
        </Style>
    </Window.Resources>
    <Grid  FlowDirection="RightToLeft">
        <Grid.RowDefinitions>
            <RowDefinition Height="30" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <controls:WindowHeader Grid.Row="0" HeaderTitle="تفاصيل الفاتورة" />
        <Border Grid.Row="1" Margin="10" CornerRadius="15" Background="White" BorderThickness="2" BorderBrush="{StaticResource PrimaryBackgroundColor}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!-- Invoice Header Info -->
                <Border Grid.Row="0" Background="#F8FAFC" Padding="20" Margin="10" CornerRadius="10">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="رقم الفاتورة" FontSize="13" Foreground="#64748B" Margin="0,0,0,5"/>
                            <TextBlock Text="{Binding InvoiceNumber}" FontSize="16" FontWeight="Bold" Foreground="#1E293B"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1">
                            <TextBlock Text="التاريخ" FontSize="13" Foreground="#64748B" Margin="0,0,0,5"/>
                            <TextBlock Text="{Binding InvoiceDate, StringFormat='{}{0:yyyy/MM/dd HH:mm}'}" FontSize="16" FontWeight="Bold" Foreground="#1E293B"/>
                        </StackPanel>

                        <StackPanel Grid.Column="2">
                            <TextBlock Text="الكاشير" FontSize="13" Foreground="#64748B" Margin="0,0,0,5"/>
                            <TextBlock Text="{Binding CashierName}" FontSize="16" FontWeight="Bold" Foreground="#6366F1"/>
                        </StackPanel>

                        <StackPanel Grid.Column="3">
                            <TextBlock Text="العميل" FontSize="13" Foreground="#64748B" Margin="0,0,0,5"/>
                            <TextBlock Text="{Binding CustomerName}" FontSize="16" FontWeight="Bold" Foreground="#059669"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Products DataGrid -->
                <DataGrid Grid.Row="2" Margin="10"
                          IsReadOnly="False"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          HeadersVisibility="All"
                          ItemsSource="{Binding ProductList}">
                    <DataGrid.Columns>
                        <DataGridTextColumn IsReadOnly="True" Header="اسم المنتج" Width="3*" Binding="{Binding Product.Name}" />
                        <DataGridTemplateColumn Header="الكمية" Width="1*">
                            <DataGridTemplateColumn.CellEditingTemplate>
                                <DataTemplate>
                                    <TextBox Text="{Binding Quantity, UpdateSourceTrigger=PropertyChanged}" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellEditingTemplate>
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Quantity}" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="سعر البيع" Width="1*">
                            <DataGridTemplateColumn.CellEditingTemplate>
                                <DataTemplate>
                                    <TextBox Text="{Binding SalePrice, UpdateSourceTrigger=PropertyChanged}" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellEditingTemplate>
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding SalePrice, StringFormat={}{0:N2}}" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="الإجمالي" Width="1*" IsReadOnly="True">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Total, StringFormat={}{0:N2}}" FontWeight="Bold" Foreground="#059669"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="التفاصيل" Width="2*">
                            <DataGridTemplateColumn.CellEditingTemplate>
                                <DataTemplate>
                                    <TextBox Text="{Binding Details, UpdateSourceTrigger=PropertyChanged}" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellEditingTemplate>
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Details}" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- Invoice Summary -->
                <Border Grid.Row="3" Background="#F8FAFC" Padding="20" Margin="10,0,10,10" CornerRadius="10">
                    <Grid HorizontalAlignment="Right">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <TextBlock Grid.Row="0" Grid.Column="0" Text="إجمالي الكمية:" FontSize="14" Foreground="#64748B" Margin="0,0,15,10"/>
                        <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding TotalQuantity}" FontSize="14" FontWeight="Bold" Margin="0,0,0,10"/>
                        
                        <TextBlock Grid.Row="1" Grid.Column="0" Text="المجموع الفرعي:" FontSize="14" Foreground="#64748B" Margin="0,0,15,10"/>
                        <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding SubTotal}" FontSize="14" FontWeight="Bold" Margin="0,0,0,10"/>
                        
                        <TextBlock Grid.Row="2" Grid.Column="0" Text="الضريبة:" FontSize="14" Foreground="#64748B" Margin="0,0,15,10"/>
                        <TextBlock Grid.Row="2" Grid.Column="1" FontSize="14" FontWeight="Bold" Margin="0,0,0,10">
                            <Run Text="{Binding TaxPercentage}"/><Run Text=" %"/>
                        </TextBlock>
                        
                        <TextBlock Grid.Row="3" Grid.Column="0" Text="الخصم:" FontSize="14" Foreground="#64748B" Margin="0,0,15,10"/>
                        <TextBlock Grid.Row="3" Grid.Column="1" FontSize="14" FontWeight="Bold" Margin="0,0,0,10">
                            <Run Text="{Binding DiscountPercentage}"/><Run Text=" %"/>
                        </TextBlock>
                        
                        <Border Grid.Row="4" Grid.ColumnSpan="2" BorderThickness="0,2,0,0" BorderBrush="#E2E8F0" Padding="0,10,0,0">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="الإجمالي:" FontSize="16" FontWeight="Bold" Foreground="#1E293B" Margin="0,0,15,0"/>
                                <TextBlock Grid.Column="1" Text="{Binding Total}" FontSize="18" FontWeight="Bold" Foreground="#059669"/>
                            </Grid>
                        </Border>
                    </Grid>
                </Border>

            </Grid>
        </Border>

    </Grid>

</Window>