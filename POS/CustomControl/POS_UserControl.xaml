<UserControl x:Class="POS.CustomControl.POS_UserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:validations="clr-namespace:POS.Validations"
             mc:Ignorable="d"
             MinWidth="1366"
             MinHeight="768"
             FontFamily="Segoe UI"
             FlowDirection="RightToLeft">

    <UserControl.Resources>
        <validations:DoubleTextConverter x:Key="DoubleTextConverter" />

        <!-- Smaller general textbox style -->
        <Style x:Key="PosTextBoxStyle" TargetType="TextBox" BasedOn="{StaticResource MaterialDesignOutlinedTextBox}">
            <Setter Property="Height" Value="32" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Margin" Value="0" />
            <Setter Property="Padding" Value="10,0" />
            <Setter Property="Foreground" Value="#111827" />
            <Setter Property="CaretBrush" Value="#111827" />
            <Setter Property="SelectionBrush" Value="#BFD7FF" />
            <Setter Property="SelectionOpacity" Value="0.7" />
            <Setter Property="materialDesign:HintAssist.IsFloating" Value="True" />
        </Style>

        <!-- Smaller cash bill buttons -->
        <Style x:Key="BillButtonStyle" TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedButton}">
            <Setter Property="Width" Value="128" />
            <Setter Property="Height" Value="64" />
            <Setter Property="Margin" Value="4" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="BorderBrush" Value="#2196F3" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5" />
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <ScaleTransform ScaleX="1" ScaleY="1" />
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="RenderTransform">
                        <Setter.Value>
                            <ScaleTransform ScaleX="1.03" ScaleY="1.03" />
                        </Setter.Value>
                    </Setter>
                </Trigger>
                <DataTrigger Binding="{Binding IsSelected}" Value="False">
                    <Setter Property="BorderThickness" Value="1" />
                    <Setter Property="BorderBrush" Value="#E0E0E0" />
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="BillBadgeStyle" TargetType="Border">
            <Setter Property="Background" Value="#FF3F51B5" />
            <Setter Property="CornerRadius" Value="8" />
            <Setter Property="HorizontalAlignment" Value="Left" />
            <Setter Property="VerticalAlignment" Value="Top" />
            <Setter Property="Margin" Value="6" />
            <Setter Property="Padding" Value="6,1" />
            <Setter Property="Visibility" Value="Visible" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsSelected}" Value="False">
                    <Setter Property="Visibility" Value="Collapsed" />
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <!-- Slightly smaller grid rows -->
        <Style x:Key="PosDataGridStyle" TargetType="DataGrid" BasedOn="{StaticResource MaterialDesignDataGrid}">
            <Setter Property="HeadersVisibility" Value="Column" />
            <Setter Property="RowHeight" Value="32" />
            <Setter Property="AlternationCount" Value="100000" />
            <Setter Property="AutoGenerateColumns" Value="False" />
            <Setter Property="CanUserAddRows" Value="False" />
            <Setter Property="CanUserResizeRows" Value="False" />
            <Setter Property="GridLinesVisibility" Value="None" />
            <Setter Property="Background" Value="White" />
            <Setter Property="RowBackground" Value="White" />
            <Setter Property="AlternatingRowBackground" Value="#F5F7FA" />
        </Style>
    </UserControl.Resources>

    <Grid Background="#FAFAFA" Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header Card (smaller) -->
        <materialDesign:Card Grid.Row="0" Padding="10" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Vertical" VerticalAlignment="Center" HorizontalAlignment="Left" FlowDirection="RightToLeft">
                    <TextBlock Text="شاشة البيع" FontSize="15" FontWeight="Bold" Margin="0,0,0,4" />
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="فاتورة:" FontWeight="SemiBold" Margin="0,0,6,0" />
                        <TextBlock FontWeight="Bold" Margin="0,0,12,0">
                            <Run Text="#" />
                            <Run Text="{Binding InvoiceNumber}" />
                        </TextBlock>
                        <TextBlock Text="التاريخ:" FontWeight="SemiBold" Margin="0,0,6,0" />
                        <TextBlock Text="{Binding InvoiceDate, StringFormat={}{0:yyyy/MM/dd HH:mm}}" Margin="0,0,12,0" />
                        <TextBlock Text="الكاشير:" FontWeight="SemiBold" Margin="0,0,6,0" />
                        <TextBlock Text="{Binding CashierName}" />
                    </StackPanel>
                </StackPanel>

                <Button Grid.Column="1"
                        Width="34"
                        Height="34"
                        Margin="6,0,0,0"
                        Background="#FFE0E0E0"
                        BorderThickness="0"
                        ToolTip="إغلاق"
                        Command="{Binding CloseWindowCommand}">
                    <materialDesign:PackIcon Kind="Close" Width="16" Height="16" />
                </Button>
            </Grid>
        </materialDesign:Card>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="6*" />
                <ColumnDefinition Width="4*" />
            </Grid.ColumnDefinitions>

            <!-- LEFT COLUMN -->
            <Grid Grid.Column="0" Margin="0,0,10,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!-- Add Product Card -->
                <materialDesign:Card Grid.Row="0" Padding="12" Margin="0,0,0,6">
                    <StackPanel>
                        <StackPanel Orientation="Vertical" Margin="0,0,0,10">
                            <TextBlock Text="إضافة منتج" FontSize="16" FontWeight="Bold" />
                            <TextBlock Text="ابحث بالاسم أو الباركود" FontSize="11" Foreground="#6B7280" Margin="0,2,0,0" />
                        </StackPanel>

                        <!-- Search Row (smaller heights) -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <Border Grid.Column="0"
                                    Width="40"
                                    Height="40"
                                    CornerRadius="8"
                                    Background="#EEF2FF"
                                    BorderBrush="#C7D2FE"
                                    BorderThickness="1"
                                    Margin="0,0,8,0">
                                <materialDesign:PackIcon Kind="BarcodeScan" Width="20" Height="20" Foreground="#1D4ED8"
                                                         VerticalAlignment="Center" HorizontalAlignment="Center" />
                            </Border>

                            <Border Grid.Column="1"
                                    BorderBrush="#D1D5DB"
                                    BorderThickness="1"
                                    CornerRadius="8"
                                    Background="White"
                                    Height="40">
                                <Grid>
                                    <TextBox x:Name="SearchTextBox"
                                             FontSize="15"
                                             Foreground="#111827"
                                             Background="Transparent"
                                             CaretBrush="#111827"
                                             Padding="12,0"
                                             VerticalContentAlignment="Center"
                                             BorderThickness="0"
                                             Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay, Delay=300}">
                                        <TextBox.InputBindings>
                                            <KeyBinding Key="Enter" Command="{Binding AcceptCommand}" />
                                        </TextBox.InputBindings>
                                    </TextBox>

                                    <TextBlock Text="🔍 ابحث باسم المنتج أو الباركود..."
                                               Foreground="#9CA3AF"
                                               FontSize="13"
                                               VerticalAlignment="Center"
                                               Margin="12,0"
                                               IsHitTestVisible="False">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Text, ElementName=SearchTextBox}" Value="">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Grid>
                            </Border>

                           <Button Grid.Column="2"
        Width="40"
        Height="40"
        Margin="8,0,0,0"
        Background="#1D4ED8"
        Foreground="White"
        ToolTip="إضافة للسلة"
        AutomationProperties.Name="إضافة منتج للسلة"
        Command="{Binding AcceptCommand}"
        BorderThickness="0"
        Cursor="Hand"
        Padding="0"
        materialDesign:ButtonAssist.CornerRadius="10"
        materialDesign:ElevationAssist.Elevation="Dp3">

    <Button.Template>
        <ControlTemplate TargetType="Button">
            <Border x:Name="Bd"
                    Background="{TemplateBinding Background}"
                    CornerRadius="10">
                <ContentPresenter HorizontalAlignment="Center"
                                  VerticalAlignment="Center"/>
            </Border>
            <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter TargetName="Bd" Property="Background" Value="#2563EB"/>
                    <Setter Property="materialDesign:ElevationAssist.Elevation" Value="Dp5"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter TargetName="Bd" Property="Background" Value="#1E40AF"/>
                    <Setter Property="materialDesign:ElevationAssist.Elevation" Value="Dp2"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter TargetName="Bd" Property="Opacity" Value="0.55"/>
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>
    </Button.Template>

    <materialDesign:PackIcon Kind="Plus" Width="18" Height="18" />
</Button>
                        </Grid>

                        <!-- Quantity + Price (smaller) -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="10" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <Border Grid.Column="0" BorderBrush="#3B82F6" BorderThickness="2" CornerRadius="8" Background="White">
                                <Grid>
                                    <TextBlock Text="الكمية المطلوبة"
                                               Foreground="#3B82F6"
                                               FontSize="11"
                                               FontWeight="SemiBold"
                                               HorizontalAlignment="Right"
                                               Margin="12,8,12,0"
                                               VerticalAlignment="Top"/>
                                    <TextBox x:Name="QuantityTextBox"
                                             Text="{Binding Quantity, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay, Converter={StaticResource DoubleTextConverter}}"
                                             BorderThickness="0"
                                             Background="Transparent"
                                             FontSize="22"
                                             FontWeight="Bold"
                                             Foreground="#1F2937"
                                             HorizontalContentAlignment="Center"
                                             VerticalContentAlignment="Center"
                                             Padding="12,24,12,8"
                                             Height="60"
                                             validations:DecimalTextBoxBehavior.AllowDecimalPoint="False"
                                             InputMethod.IsInputMethodEnabled="False"/>
                                    <TextBlock Text="أدخل الكمية"
                                               Foreground="#D1D5DB"
                                               FontSize="14"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"
                                               Margin="0,10,0,0"
                                               IsHitTestVisible="False">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Text, ElementName=QuantityTextBox}" Value="">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Grid>
                            </Border>

                            <Border Grid.Column="2" BorderBrush="#10B981" BorderThickness="2" CornerRadius="8" Background="White">
                                <Grid>
                                    <TextBlock Text="سعر الوحدة"
                                               Foreground="#10B981"
                                               FontSize="11"
                                               FontWeight="SemiBold"
                                               HorizontalAlignment="Right"
                                               Margin="12,8,12,0"
                                               VerticalAlignment="Top"/>
                                    <TextBox x:Name="SalePriceTextBox"
                                             Text="{Binding SalePrice, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay, Converter={StaticResource DoubleTextConverter}}"
                                             BorderThickness="0"
                                             Background="Transparent"
                                             FontSize="22"
                                             FontWeight="Bold"
                                             Foreground="#059669"
                                             HorizontalContentAlignment="Center"
                                             VerticalContentAlignment="Center"
                                             Padding="12,24,12,8"
                                             Height="60"
                                             validations:DecimalTextBoxBehavior.AllowDecimalPoint="True"
                                             InputMethod.IsInputMethodEnabled="False"/>
                                    <TextBlock Text="أدخل السعر"
                                               Foreground="#D1D5DB"
                                               FontSize="14"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"
                                               Margin="0,10,0,0"
                                               IsHitTestVisible="False">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Text, ElementName=SalePriceTextBox}" Value="">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Grid>
                            </Border>
                        </Grid>

                        <!-- Customer selection (smaller) -->
                        <StackPanel Margin="0,0,0,10">
                            <TextBlock Text="العميل" FontWeight="SemiBold" Margin="0,0,0,6" />
                            <TextBox Style="{StaticResource PosTextBoxStyle}"
                                     Text="{Binding CustomerSearchText, UpdateSourceTrigger=PropertyChanged}"
                                     TextAlignment="Right"
                                     FlowDirection="RightToLeft"
                                     VerticalContentAlignment="Center"
                                     materialDesign:HintAssist.Hint="ابحث باسم العميل أو رقم الهاتف"
                                     Margin="0,0,0,6" />

                            <ComboBox ItemsSource="{Binding CustomersView}"
                                      SelectedItem="{Binding SelectedCustomer}"
                                      MinHeight="32"
                                      IsEditable="True"
                                      IsReadOnly="True"
                                      StaysOpenOnEdit="True"
                                      Text="{Binding SelectedCustomer.Name, Mode=OneWay}"
                                      TextSearch.TextPath="Name"
                                      FlowDirection="RightToLeft"
                                      VerticalContentAlignment="Center"
                                      Padding="6,0"
                                      FontSize="13">
                                <ComboBox.Resources>
                                    <Style TargetType="TextBox">
                                        <Setter Property="TextAlignment" Value="Right" />
                                        <Setter Property="FontSize" Value="13" />
                                    </Style>
                                </ComboBox.Resources>
                                <ComboBox.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel>
                                            <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="13" />
                                            <TextBlock Text="{Binding Phone}" FontSize="10" Foreground="#64748B" />
                                        </StackPanel>
                                    </DataTemplate>
                                </ComboBox.ItemTemplate>
                            </ComboBox>
                        </StackPanel>

                        <!-- Product Info card (slightly smaller padding/fonts) -->
                        <Border Background="#F8FAFC" Padding="12" CornerRadius="12" BorderBrush="#E2E8F0" BorderThickness="1">
                            <StackPanel>
                                <TextBlock Text="{Binding ItemName, TargetNullValue='اختر منتج للبدء'}"
                                           FontSize="16"
                                           FontWeight="Bold"
                                           Foreground="#1E293B"
                                           TextWrapping="Wrap"/>

                                <Grid Margin="0,12,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <Border Grid.Column="0" Background="#EFF6FF" CornerRadius="8" Padding="10" Margin="0,0,6,0">
                                        <StackPanel HorizontalAlignment="Center">
                                            <TextBlock Text="المخزون المتاح" FontSize="10" Foreground="#64748B" HorizontalAlignment="Center"/>
                                            <TextBlock Text="{Binding RemainingQuantity, StringFormat={}{0:0.##}, TargetNullValue='--'}"
                                                       FontSize="20"
                                                       FontWeight="Bold"
                                                       Foreground="#2563EB"
                                                       HorizontalAlignment="Center"
                                                       Margin="0,4,0,0"/>
                                        </StackPanel>
                                    </Border>

                                    <Border Grid.Column="1" Background="#F0FDF4" CornerRadius="8" Padding="10" Margin="6,0,0,0">
                                        <StackPanel HorizontalAlignment="Center">
                                            <TextBlock Text="سعر البيع" FontSize="10" Foreground="#64748B" HorizontalAlignment="Center"/>
                                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,4,0,0">
                                                <TextBlock Text="{Binding SelectedProduct.SalePrice, StringFormat={}{0:F2}, TargetNullValue='--'}"
                                                           FontSize="20"
                                                           FontWeight="Bold"
                                                           Foreground="#16A34A"/>
                                                <TextBlock Text=" ج" FontSize="13" Foreground="#64748B" VerticalAlignment="Bottom" Margin="0,0,0,2"/>
                                            </StackPanel>
                                        </StackPanel>
                                    </Border>
                                </Grid>

                                <Border Background="#FEF3C7" CornerRadius="6" Padding="8" Margin="0,10,0,0">
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <TextBlock Text="💡" FontSize="12" Margin="0,0,6,0"/>
                                        <TextBlock Text="يمكن تعديل الكمية أو السعر قبل الإضافة"
                                                   FontSize="11"
                                                   Foreground="#92400E"/>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </materialDesign:Card>

                <!-- Cart Grid -->
                <materialDesign:Card Grid.Row="1" Margin="0,0,0,6" Padding="6">
                    <DataGrid Style="{StaticResource PosDataGridStyle}"
                              ItemsSource="{Binding CartItemsList}"
                              FlowDirection="RightToLeft">
                        <DataGrid.Columns>
                            <DataGridTemplateColumn Header="#" Width="40">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock HorizontalAlignment="Center"
                                                   Text="{Binding RelativeSource={RelativeSource AncestorType=DataGridRow}, Path=AlternationIndex}" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTextColumn Header="اسم المنتج" Width="*" Binding="{Binding Product.Name}" />
                            <DataGridTextColumn Header="الكمية" Width="80"
                                                Binding="{Binding Quantity, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                            <DataGridTextColumn Header="السعر" Width="100"
                                                Binding="{Binding SalePrice, StringFormat={}{0:F2}}" />
                            <DataGridTextColumn Header="الإجمالي" Width="110"
                                                Binding="{Binding Total, StringFormat={}{0:F2}}" />

                            <DataGridTemplateColumn Header="حذف" Width="60">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Command="{Binding DataContext.RemoveCartItemCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                CommandParameter="{Binding}"
                                                Background="Transparent"
                                                BorderThickness="0"
                                                Foreground="#F44336">
                                            <materialDesign:PackIcon Kind="Close" Width="18" Height="18" />
                                        </Button>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </materialDesign:Card>

                <!-- Summary Card -->
                <materialDesign:Card Grid.Row="2" Padding="12">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <StackPanel>
                            <TextBlock Text="عدد الأصناف:" FontSize="12" />
                            <TextBlock Text="الكمية الإجمالية:" Margin="0,3,0,0" FontSize="12" />
                            <TextBlock Text="المجموع الفرعي:" Margin="0,3,0,0" FontWeight="Bold" FontSize="12" />
                        </StackPanel>

                        <StackPanel Grid.Column="1" HorizontalAlignment="Left">
                            <TextBlock Text="{Binding CartItemsCount, StringFormat={}{0} صنف}" FontSize="12" />
                            <TextBlock Text="{Binding TotalQuantity, StringFormat={}{0:0.##}}" Margin="0,3,0,0" FontSize="12" />
                            <TextBlock Text="{Binding SubTotal, StringFormat={}{0:F2} جنيه}" Margin="0,3,0,0" FontSize="16" FontWeight="Bold" />
                        </StackPanel>
                    </Grid>
                </materialDesign:Card>
            </Grid>

            <!-- RIGHT COLUMN -->
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!-- Total Card (smaller font/padding) -->
                <materialDesign:Card Grid.Row="0" Padding="16" Margin="0,0,0,6">
                    <materialDesign:Card.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#E3F2FD" Offset="0" />
                            <GradientStop Color="#BBDEFB" Offset="1" />
                        </LinearGradientBrush>
                    </materialDesign:Card.Background>
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="المبلغ المطلوب" FontSize="13" FontWeight="SemiBold" HorizontalAlignment="Center" />
                        <TextBlock Text="{Binding TotalAmount, StringFormat={}{0:F2} جنيه}"
                                   FontSize="28"
                                   FontWeight="Bold"
                                   Foreground="#0D47A1"
                                   HorizontalAlignment="Center"
                                   Margin="0,4,0,0" />
                    </StackPanel>
                </materialDesign:Card>

                <!-- Cash bills -->
                <materialDesign:Card Grid.Row="1" Padding="12" Margin="0,0,0,6">
                    <StackPanel>
                        <TextBlock Text="الفئات النقدية" FontSize="15" FontWeight="Bold" Margin="0,0,0,10" />
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <ItemsControl ItemsSource="{Binding CashBills}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <UniformGrid Columns="2" />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Button Style="{StaticResource BillButtonStyle}"
                                                Background="{Binding Background}"
                                                Command="{Binding DataContext.AddBillCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                CommandParameter="{Binding Value}">
                                            <Grid>
                                                <Border Style="{StaticResource BillBadgeStyle}">
                                                    <TextBlock Text="{Binding Count, StringFormat=x{0}}" Foreground="White" FontSize="10" />
                                                </Border>
                                                <StackPanel VerticalAlignment="Center">
                                                    <TextBlock Text="{Binding Label}" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" />
                                                    <TextBlock Text="جنيه" FontSize="10" HorizontalAlignment="Center" />
                                                </StackPanel>
                                            </Grid>
                                        </Button>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </StackPanel>
                </materialDesign:Card>

                <!-- Manual amount (smaller) -->
                <materialDesign:Card Grid.Row="2" Padding="12" Margin="0,0,0,6">
                    <Border BorderBrush="#F59E0B" BorderThickness="2" CornerRadius="8" Background="White">
                        <Grid>
                            <TextBlock Text="أدخل المبلغ يدوياً"
                                       Foreground="#F59E0B"
                                       FontSize="11"
                                       FontWeight="SemiBold"
                                       HorizontalAlignment="Right"
                                       Margin="12,8,12,0"
                                       VerticalAlignment="Top"/>
                            <TextBox x:Name="ManualAmountTextBox"
                                     Text="{Binding ManualAmount, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay, Converter={StaticResource DoubleTextConverter}}"
                                     BorderThickness="0"
                                     Background="Transparent"
                                     FontSize="22"
                                     FontWeight="Bold"
                                     Foreground="#D97706"
                                     HorizontalContentAlignment="Center"
                                     VerticalContentAlignment="Center"
                                     Padding="12,24,12,8"
                                     Height="64"
                                     validations:DecimalTextBoxBehavior.AllowDecimalPoint="True"
                                     InputMethod.IsInputMethodEnabled="False"/>
                            <TextBlock Text="💵 المبلغ المدفوع"
                                       Foreground="#D1D5DB"
                                       FontSize="15"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       Margin="0,10,0,0"
                                       IsHitTestVisible="False">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Text, ElementName=ManualAmountTextBox}" Value="">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Grid>
                    </Border>
                </materialDesign:Card>

                <!-- Payment summary -->
                <materialDesign:Card Grid.Row="3" Padding="12" Margin="0,0,0,6">
                    <StackPanel>
                        <Grid Margin="0,0,0,6">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="المبلغ المطلوب:" FontSize="12"/>
                            <TextBlock Grid.Column="1" Text="{Binding TotalAmount, StringFormat={}{0:F2} جنيه}" FontWeight="Bold" FontSize="12"/>
                        </Grid>

                        <Grid Margin="0,0,0,6">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="المبلغ المدفوع:" FontSize="12"/>
                            <TextBlock Grid.Column="1"
                                       Text="{Binding AmountPaid, StringFormat={}{0:F2} جنيه}"
                                       FontSize="16"
                                       FontWeight="Bold">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="#F44336" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsPaymentComplete}" Value="True">
                                                <Setter Property="Foreground" Value="#4CAF50" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Grid>

                        <Grid Margin="0,0,0,6">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="الباقي:" FontSize="12"/>
                            <TextBlock Grid.Column="1"
                                       Text="{Binding ChangeDisplay}"
                                       FontSize="20"
                                       FontWeight="Bold">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="Black" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsChangePositive}" Value="True">
                                                <Setter Property="Foreground" Value="#4CAF50" />
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding IsChangeNegative}" Value="True">
                                                <Setter Property="Foreground" Value="#F44336" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Grid>

                        <Separator Margin="0,6" />
                        <TextBlock Text="الفئات المدفوعة:" FontWeight="SemiBold" FontSize="12" Margin="0,0,0,4" />
                        <TextBlock Text="{Binding BillBreakdown}"
                                   FontFamily="Consolas"
                                   FontSize="11"
                                   TextWrapping="Wrap" />
                    </StackPanel>
                </materialDesign:Card>

                <!-- Action buttons (smaller) -->
                <StackPanel Grid.Row="4">
                    <Button Height="54"
                            FontSize="16"
                            Background="#4CAF50"
                            Foreground="White"
                            Command="{Binding CompletePaymentCommand}"
                            IsEnabled="{Binding CanCompletePayment}"
                            Margin="0,0,0,6">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <materialDesign:PackIcon Kind="CheckCircle" Width="22" Height="22" Margin="0,0,8,0" />
                            <TextBlock Text="إتمام الدفع" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>

                    <Button Height="44"
                            FontSize="14"
                            Background="#2196F3"
                            Foreground="White"
                            Command="{Binding PrintInvoiceCommand}"
                            Margin="0,0,0,6">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <materialDesign:PackIcon Kind="Printer" Width="18" Height="18" Margin="0,0,8,0" />
                            <TextBlock Text="طباعة الفاتورة" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Button Grid.Column="0"
                                Height="44"
                                FontSize="14"
                                Background="#9E9E9E"
                                Foreground="White"
                                Content="مسح"
                                Command="{Binding ClearPaymentCommand}"
                                Margin="0,0,6,0" />
                        <Button Grid.Column="1"
                                Height="44"
                                FontSize="14"
                                Background="#F44336"
                                Foreground="White"
                                Content="إلغاء"
                                Command="{Binding CancelSaleCommand}"
                                Margin="6,0,0,0" />
                    </Grid>
                </StackPanel>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
