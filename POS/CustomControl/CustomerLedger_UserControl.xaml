<UserControl x:Class="POS.CustomControl.CustomerLedger_UserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:validations="clr-namespace:POS.Validations"
             mc:Ignorable="d" MinWidth="800" MinHeight="800">
    
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Assets/UltraModernStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <validations:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
            <validations:IndexPlusOneConverter x:Key="IndexPlusOneConverter"/>
            <validations:BalanceToColorConverter x:Key="BalanceToColorConverter"/>
        </ResourceDictionary>
    </UserControl.Resources>

    <UserControl.Triggers>
        <EventTrigger RoutedEvent="Loaded">
            <BeginStoryboard Storyboard="{StaticResource FadeInAnimation}"/>
        </EventTrigger>
    </UserControl.Triggers>

    <Grid Background="{StaticResource BackgroundGradientBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Header -->
            <RowDefinition Height="Auto"/> <!-- Stats -->
            <RowDefinition Height="Auto"/> <!-- Filters -->
            <RowDefinition Height="*"/>    <!-- DataGrid -->
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" CornerRadius="20" Margin="20,20,20,16" Padding="32,24">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#1E40AF" Offset="0"/>
                    <GradientStop Color="#0EA5E9" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0">
                    <TextBlock Text="كشف حساب العميل" FontSize="28" FontWeight="Bold" Foreground="White" FlowDirection="RightToLeft"/>
                    <TextBlock Text="عرض جميع المعاملات والأرصدة" FontSize="13" Foreground="#E0E7FF" Margin="0,4,0,0" FlowDirection="RightToLeft"/>
                </StackPanel>
                
                <Button Grid.Column="1" Content="تصدير" Command="{Binding ExportCommand}"
                        Style="{StaticResource CustomButtonStyle}" 
                        Background="#10B981" Foreground="White"
                        Width="120" Height="50" FontSize="14" FontWeight="SemiBold"
                        VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <!-- Stats Cards -->
        <Grid Grid.Row="1" Margin="20,0,20,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource UltraModernCard}" Padding="20,18">
                <StackPanel>
                    <TextBlock Text="إجمالي المدفوعات" FontSize="12" Foreground="{StaticResource Gray600Brush}" FlowDirection="RightToLeft"/>
                    <TextBlock Text="{Binding TotalPayments}" FontSize="32" FontWeight="ExtraBold" Foreground="{StaticResource Success600Brush}" Margin="0,8,0,0"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Style="{StaticResource UltraModernCard}" Padding="20,18">
                <StackPanel>
                    <TextBlock Text="إجمالي المشتريات" FontSize="12" Foreground="{StaticResource Gray600Brush}" FlowDirection="RightToLeft"/>
                    <TextBlock Text="{Binding TotalPurchases}" FontSize="32" FontWeight="ExtraBold" Foreground="{StaticResource Warning600Brush}" Margin="0,8,0,0"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="4" Style="{StaticResource UltraModernCard}" Padding="20,18">
                <StackPanel>
                    <TextBlock Text="الرصيد الحالي" FontSize="12" Foreground="{StaticResource Gray600Brush}" FlowDirection="RightToLeft"/>
                    <TextBlock Text="{Binding Balance}" FontSize="32" FontWeight="ExtraBold" 
                               Foreground="{Binding Balance, Converter={StaticResource BalanceToColorConverter}}" Margin="0,8,0,0"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Filters -->
        <Border Grid.Row="2" Style="{StaticResource UltraModernCard}" Margin="20,0,20,16" Padding="24,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="العميل" FontSize="13" FontWeight="SemiBold" Foreground="{StaticResource Gray700Brush}" Margin="0,0,0,8" FlowDirection="RightToLeft"/>
                    <ComboBox ItemsSource="{Binding Customers}" SelectedItem="{Binding SelectedCustomer}" 
                              DisplayMemberPath="Name"/>
                </StackPanel>

                <StackPanel Grid.Column="2">
                    <TextBlock Text="من تاريخ" FontSize="13" FontWeight="SemiBold" Foreground="{StaticResource Gray700Brush}" Margin="0,0,0,8" FlowDirection="RightToLeft"/>
                    <DatePicker SelectedDate="{Binding DateFrom}"/>
                </StackPanel>

                <StackPanel Grid.Column="4">
                    <TextBlock Text="إلى تاريخ" FontSize="13" FontWeight="SemiBold" Foreground="{StaticResource Gray700Brush}" Margin="0,0,0,8" FlowDirection="RightToLeft"/>
                    <DatePicker SelectedDate="{Binding DateTo}"/>
                </StackPanel>

                <Button Grid.Column="6" Content="بحث" Style="{StaticResource CustomButtonStyle}" 
                        Width="120" VerticalAlignment="Bottom" Command="{Binding SearchCommand}"/>
            </Grid>
        </Border>

        <!-- Transactions DataGrid -->
        <Border Grid.Row="3" Style="{StaticResource UltraModernCard}" Margin="20,0,20,20" Padding="0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Background="{StaticResource Neutral50Brush}" Padding="24,16" BorderBrush="{StaticResource Neutral200Brush}" BorderThickness="0,0,0,1">
                    <TextBlock Text="المعاملات" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource Gray900Brush}" FlowDirection="RightToLeft"/>
                </Border>

                <DataGrid Grid.Row="1" ItemsSource="{Binding LedgerEntries}"
                          AutoGenerateColumns="False" IsReadOnly="True"
                          Visibility="{Binding HasLedgerEntries, Converter={StaticResource BoolToVisibilityConverter}}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="#" Width="60" 
                                            Binding="{Binding RelativeSource={RelativeSource AncestorType=DataGridRow}, Converter={StaticResource IndexPlusOneConverter}}">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="TextAlignment" Value="Center"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                    <Setter Property="Foreground" Value="{StaticResource Gray500Brush}"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTextColumn Header="التاريخ" Width="1.5*" Binding="{Binding Date, StringFormat='{}{0:dd/MM/yyyy}'}">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="{StaticResource Gray700Brush}"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTextColumn Header="الوصف" Width="3*" Binding="{Binding Description}">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="{StaticResource Gray900Brush}"/>
                                    <Setter Property="FontWeight" Value="Medium"/>
                                    <Setter Property="FlowDirection" Value="RightToLeft"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTextColumn Header="مدين" Width="1.5*" Binding="{Binding Debit}">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="{StaticResource Danger600Brush}"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTextColumn Header="دائن" Width="1.5*" Binding="{Binding Credit}">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="{StaticResource Success600Brush}"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTextColumn Header="الرصيد" Width="1.5*" Binding="{Binding Balance}">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="{Binding Balance, Converter={StaticResource BalanceToColorConverter}}"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- Loading Indicator -->
                <Border Grid.Row="1" Background="#FFFFFFCC"
                        Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibilityConverter}}">
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                        <ProgressBar IsIndeterminate="True" Width="100" Height="100"
                                     Style="{StaticResource MaterialDesignCircularProgressBar}"/>
                        <TextBlock Text="{Binding BusyMessage}" FontSize="13" Foreground="{StaticResource Gray700Brush}"
                                   Margin="0,16,0,0" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <!-- Empty State - No Entries -->
                <StackPanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center"
                            Visibility="{Binding ShowEmptyState, Converter={StaticResource BoolToVisibilityConverter}}">
                    <Border Width="80" Height="80" CornerRadius="20" Background="{StaticResource Neutral100Brush}">
                        <TextBlock Text="📋" FontSize="40" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <TextBlock Text="لا توجد معاملات" FontSize="16" FontWeight="SemiBold"
                               Foreground="{StaticResource Gray700Brush}" Margin="0,16,0,0" HorizontalAlignment="Center"/>
                    <TextBlock Text="لم يتم تسجيل أي معاملات لهذا العميل" FontSize="13"
                               Foreground="{StaticResource Gray500Brush}" Margin="0,6,0,0" HorizontalAlignment="Center"/>
                </StackPanel>

                <!-- Select Customer Placeholder -->
                <StackPanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center"
                            Visibility="{Binding ShowSelectCustomer, Converter={StaticResource BoolToVisibilityConverter}}">
                    <Border Width="80" Height="80" CornerRadius="20" Background="{StaticResource Neutral100Brush}">
                        <TextBlock Text="👤" FontSize="40" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <TextBlock Text="اختر عميل" FontSize="16" FontWeight="SemiBold"
                               Foreground="{StaticResource Gray700Brush}" Margin="0,16,0,0" HorizontalAlignment="Center"/>
                    <TextBlock Text="اضغط على عميل لعرض معاملاته" FontSize="13"
                               Foreground="{StaticResource Gray500Brush}" Margin="0,6,0,0" HorizontalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
