<UserControl x:Class="POS.CustomControl.CustomerLedger_UserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:validations="clr-namespace:POS.Validations"
             mc:Ignorable="d" 
             MinWidth="900"
             MinHeight="700"
             FlowDirection="RightToLeft">
    <UserControl.Resources>
        <validations:DoubleTextConverter x:Key="DoubleTextConverter" />
    </UserControl.Resources>
    <Border FlowDirection="RightToLeft"
            Grid.Row="1"
            Margin="10"
            CornerRadius="15"
            Background="White"
            BorderThickness="2"
            BorderBrush="{StaticResource PrimaryBackgroundColor}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0" Margin="20,15,20,10">
                <TextBlock Text="كشف حساب عميل" FontSize="20" FontWeight="Bold"/>
                <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                    <TextBlock Text="الرصيد الحالي: " FontSize="14" Foreground="Gray"/>
                    <TextBlock FontSize="14" FontWeight="Bold">
                        <Run Text="{Binding CurrentBalance, StringFormat={}{0:N2}}"/>
                        <Run Text=" جنيه"/>
                    </TextBlock>
                </StackPanel>
            </StackPanel>

            <!-- Filters -->
            <Grid Grid.Row="1" Margin="20,0,20,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="0,0,10,0">
                    <TextBlock Text="العميل" FontSize="14" FontWeight="Bold"/>
                    <ComboBox ItemsSource="{Binding Customers}"
                              SelectedItem="{Binding SelectedCustomer}"
                              DisplayMemberPath="Name"
                              Height="35"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="0,0,10,0">
                    <TextBlock Text="من تاريخ" FontSize="14" FontWeight="Bold"/>
                    <DatePicker SelectedDate="{Binding StartDate}" Height="35"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Margin="0,0,10,0">
                    <TextBlock Text="إلى تاريخ" FontSize="14" FontWeight="Bold"/>
                    <DatePicker SelectedDate="{Binding EndDate}" Height="35"/>
                </StackPanel>

                <StackPanel Grid.Column="3" Orientation="Horizontal" VerticalAlignment="Bottom">
                    <Button Content="تحديث"
                            Style="{StaticResource CustomButtonStyle}"
                            Background="#007ACC"
                            Foreground="White"
                            Command="{Binding RefreshCommand}"/>
                    <Button Content="مسح"
                            Style="{StaticResource CustomButtonStyle}"
                            Background="#E53935"
                            Foreground="White"
                            Command="{Binding ClearFiltersCommand}"/>
                </StackPanel>
            </Grid>

            <!-- Payment -->
            <Border Grid.Row="2" Margin="20,0,20,10" CornerRadius="10" Background="#F7F7F7" Padding="10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Margin="0,0,10,0">
                        <TextBlock Text="مبلغ التحصيل" FontSize="14" FontWeight="Bold"/>
                        <TextBox Text="{Binding PaymentAmount, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay, Converter={StaticResource DoubleTextConverter}}"
                                 Style="{StaticResource ModernTextBoxStyle}"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Margin="0,0,10,0">
                        <TextBlock Text="طريقة الدفع" FontSize="14" FontWeight="Bold"/>
                        <ComboBox ItemsSource="{Binding PaymentMethods}"
                                  SelectedItem="{Binding SelectedPaymentMethod}"
                                  Height="35"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2" Margin="0,0,10,0">
                        <TextBlock Text="مرجع" FontSize="14" FontWeight="Bold"/>
                        <TextBox Text="{Binding PaymentReference, UpdateSourceTrigger=PropertyChanged}"
                                 Style="{StaticResource ModernTextBoxStyle}"/>
                    </StackPanel>

                    <Button Grid.Column="3"
                            Content="تسجيل تحصيل"
                            Style="{StaticResource CustomButtonStyle}"
                            Background="#2E7D32"
                            Foreground="White"
                            Command="{Binding RecordPaymentCommand}"
                            Margin="0,18,0,0"/>
                </Grid>
            </Border>

            <!-- Statement Table -->
            <DataGrid Grid.Row="3"
                      Margin="20"
                      ItemsSource="{Binding LedgerEntries}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      SelectionMode="Single">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="التاريخ"
                                        Binding="{Binding Date, StringFormat='{}{0:yyyy/MM/dd}'}"
                                        Width="120"/>
                    <DataGridTextColumn Header="الوصف"
                                        Binding="{Binding Description}"
                                        Width="*"/>
                    <DataGridTextColumn Header="المرجع"
                                        Binding="{Binding ReferenceNumber}"
                                        Width="140"/>
                    <DataGridTextColumn Header="مدين"
                                        Binding="{Binding Debit, StringFormat={}{0:N2}}"
                                        Width="120"/>
                    <DataGridTextColumn Header="دائن"
                                        Binding="{Binding Credit, StringFormat={}{0:N2}}"
                                        Width="120"/>
                    <DataGridTextColumn Header="الرصيد"
                                        Binding="{Binding RunningBalance, StringFormat={}{0:N2}}"
                                        Width="140"/>
                    <DataGridTextColumn Header="طريقة الدفع"
                                        Binding="{Binding PaymentMethod}"
                                        Width="130"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Border>
</UserControl>
