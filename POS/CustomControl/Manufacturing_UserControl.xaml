<UserControl x:Class="POS.CustomControl.Manufacturing_UserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:validations="clr-namespace:POS.Validations"
             mc:Ignorable="d" MinWidth="800" MinHeight="800">
    
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Assets/UltraModernStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <validations:IndexPlusOneConverter x:Key="IndexPlusOneConverter"/>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Background="{StaticResource BackgroundGradientBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" CornerRadius="20" Margin="20,20,20,16" Padding="32,24">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#C2410C" Offset="0"/>
                    <GradientStop Color="#F59E0B" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
            <StackPanel>
                <TextBlock Text="إدارة التصنيع" FontSize="28" FontWeight="Bold" Foreground="White" FlowDirection="RightToLeft"/>
                <TextBlock Text="عمليات الإنتاج واستهلاك المواد الخام" FontSize="13" Foreground="#FFEDD5" Margin="0,4,0,0" FlowDirection="RightToLeft"/>
            </StackPanel>
        </Border>

        <!-- Stats -->
        <Grid Grid.Row="1" Margin="20,0,20,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource UltraModernCard}" Padding="20,18">
                <StackPanel>
                    <TextBlock Text="أوامر الإنتاج" FontSize="12" Foreground="{StaticResource Gray600}" FlowDirection="RightToLeft"/>
                    <TextBlock Text="{Binding TotalOrders}" FontSize="32" FontWeight="ExtraBold" Foreground="{StaticResource Warning600Brush}" Margin="0,8,0,0"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Style="{StaticResource UltraModernCard}" Padding="20,18">
                <StackPanel>
                    <TextBlock Text="المواد الخام المستخدمة" FontSize="12" Foreground="{StaticResource Gray600}" FlowDirection="RightToLeft"/>
                    <TextBlock Text="{Binding RawMaterialsUsed}" FontSize="32" FontWeight="ExtraBold" Foreground="{StaticResource Danger600Brush}" Margin="0,8,0,0"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="4" Style="{StaticResource UltraModernCard}" Padding="20,18">
                <StackPanel>
                    <TextBlock Text="المنتجات المصنعة" FontSize="12" Foreground="{StaticResource Gray600}" FlowDirection="RightToLeft"/>
                    <TextBlock Text="{Binding ProductsManufactured}" FontSize="32" FontWeight="ExtraBold" Foreground="{StaticResource Success600Brush}" Margin="0,8,0,0"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Manufacturing Orders DataGrid -->
        <Border Grid.Row="2" Style="{StaticResource UltraModernCard}" Margin="20,0,20,20" Padding="0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Background="{StaticResource Gray50}" Padding="24,16" BorderBrush="{StaticResource Gray200}" BorderThickness="0,0,0,1">
                    <TextBlock Text="أوامر التصنيع" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource Gray900Brush}" FlowDirection="RightToLeft"/>
                </Border>

                <DataGrid Grid.Row="1" ItemsSource="{Binding ManufacturingOrders}" Style="{StaticResource UltraModernDataGrid}"
                          AutoGenerateColumns="False" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="#" Width="60" 
                                            Binding="{Binding RelativeSource={RelativeSource AncestorType=DataGridRow}, Converter={StaticResource IndexPlusOneConverter}}">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="TextAlignment" Value="Center"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                    <Setter Property="Foreground" Value="{StaticResource Gray500}"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTextColumn Header="رقم الأمر" Width="1.2*" Binding="{Binding OrderNumber}"/>
                        <DataGridTextColumn Header="المنتج" Width="2*" Binding="{Binding ProductName}"/>
                        <DataGridTextColumn Header="الكمية" Width="1*" Binding="{Binding Quantity}"/>
                        <DataGridTextColumn Header="التاريخ" Width="1.2*" Binding="{Binding Date, StringFormat='{}{0:dd/MM/yyyy}'}"/>
                        <DataGridTextColumn Header="الحالة" Width="1*" Binding="{Binding Status}"/>
                        <DataGridTemplateColumn Header="الإجراءات" Width="120">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <Button Style="{StaticResource UltraModernGhostButton}" Width="32" Height="32" Padding="0"
                                                Command="{Binding DataContext.ViewOrderCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                CommandParameter="{Binding}">
                                            <Path Fill="{StaticResource Primary600Brush}" Width="14" Height="14" Stretch="Uniform"
                                                  Data="M12,9A3,3 0 0,0 9,12A3,3 0 0,0 12,15A3,3 0 0,0 15,12A3,3 0 0,0 12,9M12,17A5,5 0 0,1 7,12A5,5 0 0,1 12,7A5,5 0 0,1 17,12A5,5 0 0,1 12,17M12,4.5C7,4.5 2.73,7.61 1,12C2.73,16.39 7,19.5 12,19.5C17,19.5 21.27,16.39 23,12C21.27,7.61 17,4.5 12,4.5Z"/>
                                        </Button>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>

                <Border Grid.Row="2" Padding="24,16" BorderBrush="{StaticResource Gray200}" BorderThickness="0,1,0,0">
                    <Button Content="أمر تصنيع جديد" Style="{StaticResource UltraModernSuccessButton}" 
                            Width="180" HorizontalAlignment="Center" Command="{Binding NewOrderCommand}"/>
                </Border>
            </Grid>
        </Border>
    </Grid>
</UserControl>
