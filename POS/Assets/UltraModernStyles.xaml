<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ═══════════════════════════════════════════════════════════════ -->
    <!--                    ULTRA MODERN COLOR SYSTEM                     -->
    <!-- ═══════════════════════════════════════════════════════════════ -->
    
    <!-- Primary Colors - Deep Blue Gradient -->
    <Color x:Key="Primary900">#0F172A</Color>
    <Color x:Key="Primary800">#1E293B</Color>
    <Color x:Key="Primary700">#334155</Color>
    <Color x:Key="Primary600">#475569</Color>
    <Color x:Key="Primary500">#64748B</Color>
    
    <!-- Accent Colors - Electric Blue -->
    <Color x:Key="Accent900">#1E3A8A</Color>
    <Color x:Key="Accent800">#1E40AF</Color>
    <Color x:Key="Accent700">#1D4ED8</Color>
    <Color x:Key="Accent600">#2563EB</Color>
    <Color x:Key="Accent500">#3B82F6</Color>
    <Color x:Key="Accent400">#60A5FA</Color>
    
    <!-- Success Colors -->
    <Color x:Key="Success900">#14532D</Color>
    <Color x:Key="Success700">#047857</Color>
    <Color x:Key="Success600">#059669</Color>
    <Color x:Key="Success500">#10B981</Color>
    <Color x:Key="Success400">#34D399</Color>

    <!-- Warning Colors -->
    <Color x:Key="Warning900">#7C2D12</Color>
    <Color x:Key="Warning700">#C2410C</Color>
    <Color x:Key="Warning600">#EA580C</Color>
    <Color x:Key="Warning500">#F97316</Color>
    <Color x:Key="Warning400">#FB923C</Color>
    
    <!-- Danger Colors -->
    <Color x:Key="Danger900">#7F1D1D</Color>
    <Color x:Key="Danger700">#B91C1C</Color>
    <Color x:Key="Danger600">#DC2626</Color>
    <Color x:Key="Danger500">#EF4444</Color>
    <Color x:Key="Danger400">#F87171</Color>
    
    <!-- Neutral Colors -->
    <Color x:Key="Neutral50">#FAFAFA</Color>
    <Color x:Key="Neutral100">#F5F5F5</Color>
    <Color x:Key="Neutral200">#E5E5E5</Color>
    <Color x:Key="Neutral300">#D4D4D4</Color>
    <Color x:Key="Neutral400">#A3A3A3</Color>
    <Color x:Key="Neutral500">#737373</Color>
    <Color x:Key="Neutral600">#525252</Color>
    <Color x:Key="Neutral700">#404040</Color>
    <Color x:Key="Neutral800">#262626</Color>
    <Color x:Key="Neutral900">#171717</Color>
    
    <!-- Gray Aliases (for backward compatibility) -->
    <Color x:Key="Gray900">#171717</Color>
    <Color x:Key="Gray800">#262626</Color>
    <Color x:Key="Gray700">#404040</Color>
    <Color x:Key="Gray600">#525252</Color>
    <Color x:Key="Gray500">#737373</Color>
    <Color x:Key="Gray400">#A3A3A3</Color>
    
    <!-- Gradient Brushes -->
    <LinearGradientBrush x:Key="PrimaryGradientBrush" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="{StaticResource Accent700}" Offset="0"/>
        <GradientStop Color="{StaticResource Accent500}" Offset="0.5"/>
        <GradientStop Color="#0EA5E9" Offset="1"/>
    </LinearGradientBrush>
    
    <LinearGradientBrush x:Key="SuccessGradientBrush" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="{StaticResource Success700}" Offset="0"/>
        <GradientStop Color="{StaticResource Success500}" Offset="1"/>
    </LinearGradientBrush>
    
    <LinearGradientBrush x:Key="WarningGradientBrush" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="{StaticResource Warning700}" Offset="0"/>
        <GradientStop Color="{StaticResource Warning500}" Offset="1"/>
    </LinearGradientBrush>
    
    <LinearGradientBrush x:Key="DangerGradientBrush" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="{StaticResource Danger700}" Offset="0"/>
        <GradientStop Color="{StaticResource Danger500}" Offset="1"/>
    </LinearGradientBrush>
    
    <!-- Glassmorphism Gradient -->
    <LinearGradientBrush x:Key="GlassBrush" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="#CCFFFFFF" Offset="0"/>
        <GradientStop Color="#99FFFFFF" Offset="1"/>
    </LinearGradientBrush>
    
    <!-- Background Gradients -->
    <LinearGradientBrush x:Key="BackgroundGradientBrush" StartPoint="0,0" EndPoint="0,1">
        <GradientStop Color="#F8FAFC" Offset="0"/>
        <GradientStop Color="#F1F5F9" Offset="0.5"/>
        <GradientStop Color="#E2E8F0" Offset="1"/>
    </LinearGradientBrush>
    
    <!-- Solid Color Brushes -->
    <SolidColorBrush x:Key="PrimaryBackgroundColor" Color="#F8FAFC"/>
    <SolidColorBrush x:Key="CardBackgroundColor" Color="White"/>
    <SolidColorBrush x:Key="TextPrimaryColor" Color="{StaticResource Primary900}"/>
    <SolidColorBrush x:Key="TextSecondaryColor" Color="{StaticResource Primary500}"/>
    
    <!-- Brush Resources (with Brush suffix to avoid conflicts) -->
    <!-- Primary Brushes -->
    <SolidColorBrush x:Key="Primary900Brush" Color="{StaticResource Primary900}"/>
    <SolidColorBrush x:Key="Primary800Brush" Color="{StaticResource Primary800}"/>
    <SolidColorBrush x:Key="Primary700Brush" Color="{StaticResource Primary700}"/>
    <SolidColorBrush x:Key="Primary600Brush" Color="{StaticResource Primary600}"/>
    <SolidColorBrush x:Key="Primary500Brush" Color="{StaticResource Primary500}"/>
    
    <!-- Accent Brushes -->
    <SolidColorBrush x:Key="Accent900Brush" Color="{StaticResource Accent900}"/>
    <SolidColorBrush x:Key="Accent800Brush" Color="{StaticResource Accent800}"/>
    <SolidColorBrush x:Key="Accent700Brush" Color="{StaticResource Accent700}"/>
    <SolidColorBrush x:Key="Accent600Brush" Color="{StaticResource Accent600}"/>
    <SolidColorBrush x:Key="Accent500Brush" Color="{StaticResource Accent500}"/>
    <SolidColorBrush x:Key="Accent400Brush" Color="{StaticResource Accent400}"/>
    
    <!-- Success Brushes -->
    <SolidColorBrush x:Key="Success900Brush" Color="{StaticResource Success900}"/>
    <SolidColorBrush x:Key="Success700Brush" Color="{StaticResource Success700}"/>
    <SolidColorBrush x:Key="Success600Brush" Color="{StaticResource Success600}"/>
    <SolidColorBrush x:Key="Success500Brush" Color="{StaticResource Success500}"/>
    <SolidColorBrush x:Key="Success400Brush" Color="{StaticResource Success400}"/>
    
    <!-- Warning Brushes -->
    <SolidColorBrush x:Key="Warning900Brush" Color="{StaticResource Warning900}"/>
    <SolidColorBrush x:Key="Warning700Brush" Color="{StaticResource Warning700}"/>
    <SolidColorBrush x:Key="Warning600Brush" Color="{StaticResource Warning600}"/>
    <SolidColorBrush x:Key="Warning500Brush" Color="{StaticResource Warning500}"/>
    <SolidColorBrush x:Key="Warning400Brush" Color="{StaticResource Warning400}"/>
    
    <!-- Danger Brushes -->
    <SolidColorBrush x:Key="Danger900Brush" Color="{StaticResource Danger900}"/>
    <SolidColorBrush x:Key="Danger700Brush" Color="{StaticResource Danger700}"/>
    <SolidColorBrush x:Key="Danger600Brush" Color="{StaticResource Danger600}"/>
    <SolidColorBrush x:Key="Danger500Brush" Color="{StaticResource Danger500}"/>
    <SolidColorBrush x:Key="Danger400Brush" Color="{StaticResource Danger400}"/>
    
    <!-- Additional Utility Brushes -->
    <SolidColorBrush x:Key="Gray900Brush" Color="{StaticResource Primary900}"/>
    <SolidColorBrush x:Key="Gray800Brush" Color="{StaticResource Neutral800}"/>
    <SolidColorBrush x:Key="Gray700Brush" Color="{StaticResource Neutral700}"/>
    <SolidColorBrush x:Key="Gray600Brush" Color="{StaticResource Neutral600}"/>
    <SolidColorBrush x:Key="Gray500Brush" Color="{StaticResource Neutral500}"/>
    <SolidColorBrush x:Key="Gray400Brush" Color="{StaticResource Neutral400}"/>
    <SolidColorBrush x:Key="WhiteBrush" Color="White"/>
    <SolidColorBrush x:Key="TransparentWhite20" Color="White" Opacity="0.2"/>
    <SolidColorBrush x:Key="TransparentWhite30" Color="White" Opacity="0.3"/>
    
    <!-- Neutral Color Brushes -->
    <SolidColorBrush x:Key="Neutral50Brush" Color="{StaticResource Neutral50}"/>
    <SolidColorBrush x:Key="Neutral100Brush" Color="{StaticResource Neutral100}"/>
    <SolidColorBrush x:Key="Neutral200Brush" Color="{StaticResource Neutral200}"/>
    <SolidColorBrush x:Key="Neutral300Brush" Color="{StaticResource Neutral300}"/>
    <SolidColorBrush x:Key="Neutral400Brush" Color="{StaticResource Neutral400}"/>
    <SolidColorBrush x:Key="Neutral500Brush" Color="{StaticResource Neutral500}"/>
    <SolidColorBrush x:Key="Neutral600Brush" Color="{StaticResource Neutral600}"/>
    <SolidColorBrush x:Key="Neutral700Brush" Color="{StaticResource Neutral700}"/>
    <SolidColorBrush x:Key="Neutral800Brush" Color="{StaticResource Neutral800}"/>
    <SolidColorBrush x:Key="Neutral900Brush" Color="{StaticResource Neutral900}"/>
    
    <!-- Light Pastel Brushes for Backgrounds -->
    <SolidColorBrush x:Key="LightBlueBrush" Color="#EEF2FF"/>
    <SolidColorBrush x:Key="VeryLightBlueBrush" Color="#EFF6FF"/>
    <SolidColorBrush x:Key="PaleBlueBrush" Color="#E0E7FF"/>
    <SolidColorBrush x:Key="LightGreenBrush" Color="#D1FAE5"/>
    <SolidColorBrush x:Key="VeryLightGreenBrush" Color="#ECFDF5"/>
    <SolidColorBrush x:Key="PaleGreenBrush" Color="#DCFCE7"/>
    <SolidColorBrush x:Key="LightYellowBrush" Color="#FEF3C7"/>
    <SolidColorBrush x:Key="LightOrangeBrush" Color="#FFEDD5"/>
    <SolidColorBrush x:Key="LightPinkBrush" Color="#FCE7F3"/>
    <SolidColorBrush x:Key="LightPurpleBrush" Color="#F3E8FF"/>
    <SolidColorBrush x:Key="PalePurpleBrush" Color="#E9D5FF"/>
    <SolidColorBrush x:Key="LightGrayBrush" Color="#E2E8F0"/>
    
    <!-- ═══════════════════════════════════════════════════════════════ -->
    <!--                       ANIMATION STORYBOARDS                      -->
    <!-- ═══════════════════════════════════════════════════════════════ -->
    
    <!-- Fade In Animation -->
    <Storyboard x:Key="FadeInAnimation">
        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                         From="0" To="1" 
                         Duration="0:0:0.4">
            <DoubleAnimation.EasingFunction>
                <CubicEase EasingMode="EaseOut"/>
            </DoubleAnimation.EasingFunction>
        </DoubleAnimation>
    </Storyboard>
    
    <!-- ═══════════════════════════════════════════════════════════════ -->
    <!--                    ULTRA MODERN BUTTON STYLES                    -->
    <!-- ═══════════════════════════════════════════════════════════════ -->
    
    <!-- Primary Gradient Button -->
    <Style x:Key="UltraModernPrimaryButton" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource PrimaryGradientBrush}"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Padding" Value="24,12"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Grid>
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                CornerRadius="12"
                                RenderTransformOrigin="0.5,0.5">
                            <Border.RenderTransform>
                                <ScaleTransform x:Name="scaleTransform" ScaleX="1" ScaleY="1"/>
                            </Border.RenderTransform>
                            <Border.Effect>
                                <DropShadowEffect x:Name="shadow" 
                                                  Color="{StaticResource Accent600}" 
                                                  BlurRadius="20" 
                                                  ShadowDepth="0" 
                                                  Opacity="0.4"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"
                                            Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="scaleTransform" 
                                                       Storyboard.TargetProperty="ScaleX" 
                                                       To="1.05" 
                                                       Duration="0:0:0.2">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseOut"/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                        <DoubleAnimation Storyboard.TargetName="scaleTransform" 
                                                       Storyboard.TargetProperty="ScaleY" 
                                                       To="1.05" 
                                                       Duration="0:0:0.2">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseOut"/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                        <DoubleAnimation Storyboard.TargetName="shadow" 
                                                       Storyboard.TargetProperty="BlurRadius" 
                                                       To="30" 
                                                       Duration="0:0:0.2"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="scaleTransform" 
                                                       Storyboard.TargetProperty="ScaleX" 
                                                       To="1" 
                                                       Duration="0:0:0.2"/>
                                        <DoubleAnimation Storyboard.TargetName="scaleTransform" 
                                                       Storyboard.TargetProperty="ScaleY" 
                                                       To="1" 
                                                       Duration="0:0:0.2"/>
                                        <DoubleAnimation Storyboard.TargetName="shadow" 
                                                       Storyboard.TargetProperty="BlurRadius" 
                                                       To="20" 
                                                       Duration="0:0:0.2"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="scaleTransform" 
                                                       Storyboard.TargetProperty="ScaleX" 
                                                       To="0.95" 
                                                       Duration="0:0:0.1"/>
                                        <DoubleAnimation Storyboard.TargetName="scaleTransform" 
                                                       Storyboard.TargetProperty="ScaleY" 
                                                       To="0.95" 
                                                       Duration="0:0:0.1"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
    <!-- Success Button -->
    <Style x:Key="UltraModernSuccessButton" TargetType="Button" BasedOn="{StaticResource UltraModernPrimaryButton}">
        <Setter Property="Background" Value="{StaticResource SuccessGradientBrush}"/>
    </Style>
    
    <!-- Warning Button -->
    <Style x:Key="UltraModernWarningButton" TargetType="Button" BasedOn="{StaticResource UltraModernPrimaryButton}">
        <Setter Property="Background" Value="{StaticResource WarningGradientBrush}"/>
    </Style>
    
    <!-- Danger Button -->
    <Style x:Key="UltraModernDangerButton" TargetType="Button" BasedOn="{StaticResource UltraModernPrimaryButton}">
        <Setter Property="Background" Value="{StaticResource DangerGradientBrush}"/>
    </Style>
    
    <!-- Ghost Button (Outline) -->
    <Style x:Key="UltraModernGhostButton" TargetType="Button">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource Accent600Brush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Accent600Brush}"/>
        <Setter Property="BorderThickness" Value="2"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Padding" Value="24,12"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border" 
                            Background="{TemplateBinding Background}" 
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="12"
                            RenderTransformOrigin="0.5,0.5">
                        <Border.RenderTransform>
                            <ScaleTransform x:Name="scaleTransform" ScaleX="1" ScaleY="1"/>
                        </Border.RenderTransform>
                        <ContentPresenter HorizontalAlignment="Center" 
                                        VerticalAlignment="Center"
                                        Margin="{TemplateBinding Padding}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#E0E7FF"/>
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="scaleTransform" 
                                                       Storyboard.TargetProperty="ScaleX" 
                                                       To="1.05" 
                                                       Duration="0:0:0.2"/>
                                        <DoubleAnimation Storyboard.TargetName="scaleTransform" 
                                                       Storyboard.TargetProperty="ScaleY" 
                                                       To="1.05" 
                                                       Duration="0:0:0.2"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="scaleTransform" 
                                                       Storyboard.TargetProperty="ScaleX" 
                                                       To="1" 
                                                       Duration="0:0:0.2"/>
                                        <DoubleAnimation Storyboard.TargetName="scaleTransform" 
                                                       Storyboard.TargetProperty="ScaleY" 
                                                       To="1" 
                                                       Duration="0:0:0.2"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Background">
                                <Setter.Value>
                                    <SolidColorBrush Color="{StaticResource Accent600}" Opacity="0.1"/>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
    <!-- ═══════════════════════════════════════════════════════════════ -->
    <!--                    ULTRA MODERN TEXTBOX STYLES                   -->
    <!-- ═══════════════════════════════════════════════════════════════ -->
    
    <Style x:Key="UltraModernTextBox" TargetType="TextBox">
        <Setter Property="Background" Value="White"/>
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryColor}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Neutral200Brush}"/>
        <Setter Property="BorderThickness" Value="2"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Padding" Value="16,12"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border x:Name="border" 
                            Background="{TemplateBinding Background}" 
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="10">
                        <Border.Effect>
                            <DropShadowEffect x:Name="shadow" 
                                            Color="#E2E8F0" 
                                            BlurRadius="8" 
                                            ShadowDepth="0" 
                                            Opacity="0"/>
                        </Border.Effect>
                        <ScrollViewer x:Name="PART_ContentHost" 
                                    Margin="{TemplateBinding Padding}"
                                    VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource Accent600Brush}"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource Neutral100Brush}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource Neutral100Brush}"/>
                            <Setter TargetName="border" Property="Opacity" Value="0.6"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
    <!-- ═══════════════════════════════════════════════════════════════ -->
    <!--                    ULTRA MODERN CARD STYLES                      -->
    <!-- ═══════════════════════════════════════════════════════════════ -->
    
    <Style x:Key="UltraModernCard" TargetType="Border">
        <Setter Property="Background" Value="White"/>
        <Setter Property="CornerRadius" Value="16"/>
        <Setter Property="Padding" Value="20"/>
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect Color="#CBD5E1" BlurRadius="16" ShadowDepth="0" Opacity="0.3"/>
            </Setter.Value>
        </Setter>
    </Style>
    
    <!-- Glassmorphism Card -->
    <Style x:Key="GlassmorphismCard" TargetType="Border">
        <Setter Property="Background">
            <Setter.Value>
                <SolidColorBrush Color="White" Opacity="0.7"/>
            </Setter.Value>
        </Setter>
        <Setter Property="CornerRadius" Value="20"/>
        <Setter Property="Padding" Value="24"/>
        <Setter Property="BorderBrush">
            <Setter.Value>
                <SolidColorBrush Color="White" Opacity="0.4"/>
            </Setter.Value>
        </Setter>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect Color="#94A3B8" BlurRadius="24" ShadowDepth="0" Opacity="0.2"/>
            </Setter.Value>
        </Setter>
    </Style>
    
    <!-- Neumorphism Card -->
    <Style x:Key="NeumorphismCard" TargetType="Border">
        <Setter Property="Background" Value="#F1F5F9"/>
        <Setter Property="CornerRadius" Value="20"/>
        <Setter Property="Padding" Value="24"/>
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect Color="#FFFFFF" BlurRadius="16" ShadowDepth="-8" Opacity="0.5" Direction="135"/>
            </Setter.Value>
        </Setter>
    </Style>
    
    <!-- ═══════════════════════════════════════════════════════════════ -->
    <!--                    ULTRA MODERN DATAGRID STYLES                  -->
    <!-- ═══════════════════════════════════════════════════════════════ -->
    
    <Style x:Key="UltraModernDataGrid" TargetType="DataGrid">
        <Setter Property="Background" Value="White"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="RowHeight" Value="56"/>
        <Setter Property="ColumnHeaderHeight" Value="60"/>
        <Setter Property="GridLinesVisibility" Value="None"/>
        <Setter Property="HeadersVisibility" Value="Column"/>
        <Setter Property="AlternatingRowBackground" Value="#FAFAFA"/>
        <Setter Property="RowBackground" Value="White"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="CanUserAddRows" Value="False"/>
        <Setter Property="CanUserDeleteRows" Value="False"/>
        <Setter Property="AutoGenerateColumns" Value="False"/>
        <Setter Property="SelectionMode" Value="Single"/>
        <Setter Property="SelectionUnit" Value="FullRow"/>
        
        <Setter Property="RowStyle">
            <Setter.Value>
                <Style TargetType="DataGridRow">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="BorderThickness" Value="0,0,0,1"/>
                    <Setter Property="BorderBrush" Value="#F1F5F9"/>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#EFF6FF"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#DBEAFE"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Setter.Value>
        </Setter>
        
        <Setter Property="CellStyle">
            <Setter.Value>
                <Style TargetType="DataGridCell">
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Padding" Value="16,8"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="DataGridCell">
                                <Border Background="{TemplateBinding Background}" 
                                       BorderThickness="{TemplateBinding BorderThickness}"
                                       Padding="{TemplateBinding Padding}">
                                    <ContentPresenter VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Foreground" Value="{StaticResource TextPrimaryColor}"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Setter.Value>
        </Setter>
        
        <Setter Property="ColumnHeaderStyle">
            <Setter.Value>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#F8FAFC"/>
                    <Setter Property="Foreground" Value="{StaticResource Primary600}"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                    <Setter Property="FontSize" Value="13"/>
                    <Setter Property="Padding" Value="16,16"/>
                    <Setter Property="BorderThickness" Value="0,0,0,2"/>
                    <Setter Property="BorderBrush" Value="#E2E8F0"/>
                    <Setter Property="HorizontalContentAlignment" Value="Right"/>
                </Style>
            </Setter.Value>
        </Setter>
    </Style>
    
    <!-- ═══════════════════════════════════════════════════════════════ -->
    <!--                    ANIMATION RESOURCES                           -->
    <!-- ═══════════════════════════════════════════════════════════════ -->
    
    <!-- Slide In From Right Animation -->
    <Storyboard x:Key="SlideInFromRightAnimation">
        <DoubleAnimation From="50" To="0" Duration="0:0:0.5" 
                        Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)">
            <DoubleAnimation.EasingFunction>
                <BackEase EasingMode="EaseOut" Amplitude="0.3"/>
            </DoubleAnimation.EasingFunction>
        </DoubleAnimation>
        <DoubleAnimation From="0" To="1" Duration="0:0:0.5" 
                        Storyboard.TargetProperty="Opacity">
            <DoubleAnimation.EasingFunction>
                <CubicEase EasingMode="EaseOut"/>
            </DoubleAnimation.EasingFunction>
        </DoubleAnimation>
    </Storyboard>
    
    <!-- Pulse Animation -->
    <Storyboard x:Key="PulseAnimation" RepeatBehavior="Forever" AutoReverse="True">
        <DoubleAnimation From="1" To="1.1" Duration="0:0:1" 
                        Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)">
            <DoubleAnimation.EasingFunction>
                <SineEase EasingMode="EaseInOut"/>
            </DoubleAnimation.EasingFunction>
        </DoubleAnimation>
        <DoubleAnimation From="1" To="1.1" Duration="0:0:1" 
                        Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)">
            <DoubleAnimation.EasingFunction>
                <SineEase EasingMode="EaseInOut"/>
            </DoubleAnimation.EasingFunction>
        </DoubleAnimation>
    </Storyboard>

</ResourceDictionary>
